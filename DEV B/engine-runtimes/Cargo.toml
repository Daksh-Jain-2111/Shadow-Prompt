[package]
name = "engine-runtime"
version = "0.1.0"
edition = "2024"

[dependencies]
aho-corasick = "1.1"
engine-detect-regex = { path = "../engine-detect-regex" }
engine-detect-ner = { path = "../engine-detect-ner" }
engine-mask-substitute = { path = "../engine-mask-substitute" }
log = "0.4"
regex = "1"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
swift-bridge = "0.1"

